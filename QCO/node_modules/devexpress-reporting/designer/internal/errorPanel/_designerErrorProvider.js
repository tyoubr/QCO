/**
* DevExpress HTML/JS Reporting (designer\internal\errorPanel\_designerErrorProvider.js)
* Version:  23.2.3
* Build date: Dec 1, 2023
* Copyright (c) 2012 - 2023 Developer Express Inc. ALL RIGHTS RESERVED
* License: https://www.devexpress.com/Support/EULAs/universal.xml
*/
import { ajax } from '@devexpress/analytics-core/analytics-internal';
import { Disposable } from '@devexpress/analytics-core/analytics-utils';
import * as ko from 'knockout';
import { HandlerUri } from '../../utils/settings';
export class DesignerErrorProvider extends Disposable {
    constructor(_report) {
        super();
        this._report = _report;
        this.errors = ko.observableArray([]);
    }
    collectErrors() {
        return ajax(HandlerUri(), 'getDesignErrors', encodeURIComponent(JSON.stringify({
            'XtraReportsLayoutSerializer': this._report.serialize()
        }))).done((result) => {
            this.errors(result);
        });
    }
}
